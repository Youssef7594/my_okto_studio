<header id="header">
    <nav class="navbar border-bottom border-body" style="background-color: black;">
        <div class="container d-flex justify-content-between">
            <div class="nav-bar">
                <a href="{{ path('app_home') }}"><img src="{{ asset('img/logo.png') }}" alt="?"></a>
            </div>

            

            <div class="d-flex justify-content-around nav-bar" id="menu">
                <div>
                    <a class="nav-link" aria-current="page" href="{{ path('app_home') }}">Home</a>
                </div>
                <div>
                    <a class="nav-link" href="{{ path('app_home') }}#tarifs">Tarifs</a>
                </div>
            
                <div>
                    <a class="nav-link" href="{{ path('app_home') }}#about">À propos</a>
                </div>

                <div>
                    <a class="nav-link" href="{{ path('app_home') }}#contact">Contact</a>
                </div>

                <div>
                    <a class="nav-link" href="{{ path('app_enregistrement') }}">Reserver</a>
                </div>
            </div>
        </div>
    </nav>
</header>